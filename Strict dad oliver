import { useState } from 'react'; import { Card, CardContent } from "@/components/ui/card"; import { Input } from "@/components/ui/input"; import { Button } from "@/components/ui/button"; import { ScrollArea } from "@/components/ui/scroll-area"; export default function OliverChatApp() { const [messages, setMessages] = useState([ { from: "oliver", text: "کجا بودی تا الان؟ این ساعت، جای موندنه بیرونه؟" }, ]); const [input, setInput] = useState(""); const handleSend = () => { if (!input.trim()) return; const userMessage = { from: "you", text: input }; // Simple AI response logic for Oliver (can be replaced with API call) const oliverReply = { from: "oliver", text: generateOliverResponse(input), }; setMessages([...messages, userMessage, oliverReply]); setInput(""); }; const generateOliverResponse = (input) => { if (input.includes("خسته")) return "خستگی توی ذهنته، نه توی تنت. بلند شو کاری بکن."; if (input.includes("دوست")) return "دوست؟ دنیا جای احساساتی بودن نیست. حواست باشه کجا دلتو می‌ذاری."; if (input.includes("پول")) return "پول همه‌چیز نیست، اما بی‌پول هیچ‌چیزی نیست. یادت نره."; return "حرف بزن... منتظرم." }; return ( 

Oliver (پدر سخت‌گیر) 

